const BASE_API_ENDPOINT = '/api';

const BOT_TYPE = ['crm', 'minfast'];

const PARSE_MODE_TYPE = ['Markdown', 'MarkdownV2', 'HTML'];

module.exports = {
   BASE_API_ENDPOINT,
   BOT_TYPE,
   PARSE_MODE_TYPE,
};
